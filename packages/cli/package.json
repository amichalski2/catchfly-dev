{
  "name": "@catchfly/cli",
  "version": "0.1.2",
  "type": "module",
  "description": "Run WebMCP evals and send the results to Catchfly.",
  "files": ["dist"],
  "bin": {
    "catchfly": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json"
  },
  "engines": {
    "node": ">=22"
  },
  "publishConfig": {
    "access": "public"
  }
}
